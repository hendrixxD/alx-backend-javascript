// promise intro
var promise = new promise(function(resolve, reject){
    // do somthing
    if (// if everthing good){
        resolve("everything good");
    }
    else{
        reject(Error("it broke, didnt go well"));
    }
});
